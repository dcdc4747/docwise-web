# 代码架构图（当前阶段）

> **约定（长期）**：本图是当前代码架构的权威快照。**改代码（尤其模块 / 接口 / 数据结构 / 路由 / 引擎层）后，必须同步更新本图**，方便后续 AI 一看就懂、能深入底层。与本仓库 `AGENTS.md` 的"后端结构"一节保持一致；相应的长期记忆在顶层 `AGENTS.md` 的"代码架构图"一节。

## 分层架构

```
前端 SPA（Vue3 + Naive UI，App.vue 单文件；PWA：manifest + 最小 SW，可装主屏/离线）
   │  **同源部署**：后端直接托管 frontend/dist，一个地址同时给页面与接口（webapp.py），
   │  此时 apiUrl() 退化成相对路径、CORS 用不上；不同源时才需要 ?apiBase= / VITE_API_BASE
   │  **未登录先出登录页**；已登录后：authFetch（Bearer 请求头）+ 票据（?ticket=，给带不了头的请求）
   │  HEAD/GET /api/tasks/{id}/files/{mono|dual}、EventSource /api/tasks/{id}/events
   │  任务动作：取消（进行中）/ 重试（失败·已取消）/ 删除（两步确认）
   │  进度：SSE 推"阶段 + 引擎自报分页进度 + 预计剩余"；拿不到就显示"引擎还没报进度"（不编百分比）
   ▼
FastAPI（main.py：启动/健康检查/CORS/挂载路由与前端产物；routers/：tasks 任务与进度、ask 问答、auth 账号、admin 后台；deps.py 统一鉴权守卫；/api/health 真检查）
   │  enqueue(task_id)；POST /api/tasks/{id}/ask → llm.answer_question（全量上下文 / FTS5 兜底）
   ▼
worker（TranslationWorker：单进程串行 + 行锁认领 + 线程池跑引擎 + 事件总线 + **取消信号表** + **进度搬运工**；心跳 + 周期扫表 + 单任务异常不杀循环）
   │  get_engine(tier).translate(request, cancel) 子进程（Popen + 轮询取消 → terminate）
   │  进度：progress.py 解析 data/outputs/{id}/engine.log 里引擎的 tqdm 进度条（解析不出就返回 None）
   ▼
engine/（可插拔：base 接口 → open_source 快档 / medium 中档 / registry 按档位选）
   ▼
storage.py（磁盘治理：data/uploads、data/outputs/{task_id}、data/backups；删任务/重试清文件、启动回收孤儿目录）
   ▼
SQLite（WAL + busy_timeout；users / auth_sessions / auth_tickets / tasks(user_id, stage, eta_seconds, started_at, finished_at) / task_history / task_blocks / task_understanding / task_blocks_fts[FTS5 trigram]）
   ▼
日志（logging_setup：data/logs/docwise.log，5MB 轮转 ×3；写不了文件自动降级为控制台）+ 引擎日志（data/outputs/{task_id}/engine.log，既是失败诊断来源，也是"真进度"的数据来源）
```

## 模块依赖（Python import 拓扑）

```
配置/存储层：
  config.py ──► pydantic_settings（.env；含 DEEPSEEK_* 与 DOCWISE_ENGINE_*）
  db.py     ──► config；SQLAlchemy engine/session（get_db）；SQLite PRAGMA（WAL+busy_timeout）；ensure_fts 建 FTS5 表+触发器
  models.py ──► db；engine.base.BlockState   # Task(+user_id) / TaskHistory / TaskBlock / TaskUnderstanding / User / AuthSession / AuthTicket
  deps.py   ──► auth/config/db/models  # 依赖注入 + 守卫：get_current_user / get_user_for_files(票据) / get_user_for_events(票据) / load_owned_task / require_admin
  auth.py   ──► config/models  # 账号核心逻辑：scrypt 密码哈希、会话令牌（只存 sha256）、临时票据、登录限流
  storage.py ──► config        # 磁盘治理：uploads / outputs/{task_id} / backups 三个目录的唯一约定 + 删除与孤儿回收
  progress.py ──► stdlib       # 诚实进度：解析引擎日志里的 tqdm 进度条（页进度 / 速率 / 预计剩余），解析不出返回 None
  webapp.py ──► config；starlette  # 同源部署：把 frontend/dist 挂到 "/"（SPA 回退 + 入口文件不缓存 + 局域网地址提示），缺产物自动跳过
  logging_setup.py ──► config  # 根日志：轮转文件 + 控制台（写不了文件自动降级）
引擎层：
  base.py        ──► stdlib   ABC TranslationEngine + TranslateRequest(output_dir)/TranslationResult + 状态枚举 + **CancelToken**
  open_source.py ──► base      # 快档：Popen 调外部翻译脚本 + 轮询取消（terminate 进程树）+ 引擎日志落盘/失败带日志尾部
  medium.py      ──► open_source  # 中档：继承快档、换 DOCWISE_ENGINE_MEDIUM_* 前缀
  registry.py    ──► base/open_source/medium  # _ENGINES 按 Tier 选引擎；PRECISE 当前映射 medium
  __init__.py    ──► 以上全部，对外 re-export
worker/路由层：
  worker.py ──► db；engine；models；storage；progress   # 扫表恢复/行锁认领/线程池跑引擎/取消信号表/进度搬运/落库/发布事件（与归属无关：按状态认领）
  main.py   ──► config；db；models；worker；storage；routers.{tasks,ask,auth,admin}   # 启动初始化 + 健康检查 + CORS + 路由挂载
  llm.py    ──► config；httpx 调 DeepSeek：extract_understanding（导读/术语）+ answer_question（问答，block_id 出处）
  routers/tasks.py  ──► deps；engine；llm；models；storage；progress  # 任务全家桶：上传/列表/详情/文件/SSE/理解层 + 删除·重试·取消 + 进度字段
  routers/ask.py    ──► deps；llm；models   # POST /api/tasks/{id}/ask：归属校验 + 全量上下文 ≤ 阈值；超阈值 FTS5 + 2 字 LIKE 兜底
  routers/auth.py   ──► auth；deps；models  # 注册/登录/退出/me/改密/票据/演示一键登录（首个注册账号自动成为管理员）
  routers/admin.py  ──► auth；deps；models  # 管理后台：账号列表、任务总览、重置密码、停用启用、角色调整
  scripts/backup_db.py ──► storage；config  # 数据库备份：VACUUM INTO + 校验 + 只留最近 N 份（不用 copy 文件，WAL 会丢数据）
  scripts/reset_password.py ──► auth；db    # 重置密码 / 提升管理员 / 列账号
前端：
  main.js  ──► App.vue；style.css；api.js（PROD 下注册 /sw.js）
  api.js   ──► API base 解析：?apiBase= > localStorage > VITE_API_BASE > 同源相对
  auth.js  ──► 令牌存取 + authFetch（401 统一回登录页）+ urlWithTicket（给 SSE/iframe/下载换票据）
  progressText.js ──► 纯函数：诚实进度文案（排队位置/页进度/已用/预计剩余），App.vue 用 computed 包一层
  App.vue  ──► 未登录 → LoginView；已登录 → 阅读工作台 + 顶栏用户区；showAdmin 时渲染 AdminView
  components/LoginView.vue ──► 登录 / 注册 / 记住我 / 演示一键进入
  components/AdminView.vue ──► 账号列表（重置密码 / 停用 / 角色）+ 全部任务表
  public/  ──► manifest.webmanifest + sw.js + icons/（PWA：装主屏/全屏/离线壳；改壳后记得升 CACHE_NAME）
  tests/   ──► vitest + happy-dom：progressText 单测 + App.vue 渲染冒烟（断言页面不出现源码痕迹）
```

## 核心数据流（上传 → 出稿 → 预览）

```
① POST /api/tasks/upload：存文件到 data/uploads/ → 建 Task(pending) → worker.enqueue 返回 202
② worker._loop：_claim(pending→in_progress,行锁,清旧块,记 started_at/stage) → _load_request（带 output_dir=data/outputs/{task_id}）
      → get_engine(tier).translate(request, cancel)(线程池) → 引擎 Popen→result.json(mono/dual+blocks) + engine.log
      → 同时起"进度搬运工"：每 1.5s 解析 engine.log 的 tqdm → 写 Task.progress/eta_seconds + bus.publish(progress)
      → _persist_result(写 TaskBlock + 终态/进度/双路径 + finished_at) → bus.publish(completed/failed)
③ 前端 startProgress：EventSource /events → applyEvent(progress/status/eta/页进度)
      → completed 后 refreshWorkbench(GET /api/tasks/{id} 一次取全) → iframe / 下载
④ 获取结果：GET /files/{mono|dual}[?download]；历史 GET /api/tasks；详情 GET /api/tasks/{id}→blocks + files_ready + understanding_status（工作台一次取全）
⑤ 导读/术语：GET /api/tasks/{id}/understanding（取缓存/状态）；POST 同路径（惰性：未算则调 extract_understanding(带 block_id) 抽取并缓存，成功 ready、失败 failed）
⑥ 论文问答：POST /api/tasks/{id}/ask {question} → 总字符 ≤ docwise_ask_full_context_max_chars 走全量上下文；超阈值走 FTS5 trigram 召回 Top8（**必须限定本任务**，2 字词 LIKE 兜底同样限定）→ answer_question 按 block_id 引用（出处 id 经存在性校验）；召不回/失败诚实返回，不编造
⑦ 回访（补门）：历史行"继续读" → openWorkbench(task) 与上传后走同一条路径 → 工作台四页签 + 溯源；「详情」按钮才打开分块状态抽屉
⑧ 运维（保命基建）：启动初始化日志 → 全链路打点（入队/开始/结束用时/失败堆栈）；worker 心跳 + 空闲每 30s 扫表兜底；GET /api/health 真检查（DB 写探测 + 心跳新鲜度 + 队列长度）
⑨ 账号与鉴权（B 批）：未登录 → 登录页；POST /api/auth/register|login → 返回令牌（库中只存 sha256）→ 之后所有任务接口带 `Authorization: Bearer`；
      进度推送/PDF 预览/下载这三种"带不了请求头"的请求 → 先 POST /api/auth/ticket 换票据（绑定 用户+任务+用途，60 秒）→ 拼 `?ticket=`；
      所有任务接口按归属过滤（非本人 404）；`POST /api/tasks`（传服务器路径的旧入口）已删除；存量无主任务启动时归属 `DOCWISE_LEGACY_OWNER`
⑩ 任务生命周期（E 批）：
      **取消** POST /api/tasks/{id}/cancel → 跑着的：worker.request_cancel 置 CancelToken → 引擎线程轮询到就 terminate 子进程 → 引擎回 CANCELLED → worker 落库 cancelled 并发事件；
        排队中的（还没被认领）：条件 UPDATE（WHERE status='pending'）直接置 cancelled，worker 认领时自然跳过；
      **重试** POST /api/tasks/{id}/retry（仅 failed/cancelled）→ 清旧块与旧导读 + 删上一轮产物 + 状态回 pending + 重新入队（task_history 保留，能看出重试过几次）；
      **删除** DELETE /api/tasks/{id} → 先叫停（在跑的话）→ 手工清子表（块/历史/理解/票据，**没有外键级联**）→ 删任务行 → 删上传原件与 data/outputs/{id}；
        子进程死后 worker 发现任务已不存在，会再清一次输出目录（kill 瞬间可能刚写了东西）；
      **磁盘治理** 启动时回收孤儿结果目录（不属于任何任务）+ 陈旧引擎临时目录；`scripts/backup_db.py` 用 VACUUM INTO 备份（WAL 下直接拷 .db 会丢最近改动）
⑪ 诚实进度（F 批）：
      引擎（子进程）翻译时用 tqdm 往 stderr 打进度条，`open_source.py` 把 stdout/stderr 一起重定向到
      `data/outputs/{task_id}/engine.log` → `progress.py` 按 `[\\r\\n]+` 切开取**最后一条**：
      `40%|██  | 2/5 [00:01<00:01, 1.72it/s]` → 页进度 2/5、速率、引擎自报的预计剩余；
      worker 里的"进度搬运工"每 1.5s 搬一次（**只增不减**：引擎分阶段重跑时进度条会回到 0，落库取历史最大值），
      写 `tasks.progress/eta_seconds/stage` + 推 SSE；详情接口另外**当场**从日志解析一份 `engine_progress`（比库里那份新，最多差 1.5 秒）；
      排队中的任务给 `queue_position`（前面还有几篇 = 正在跑的 + id 更小的 pending）；
      **解析不出就什么都不显示**——前端显示"引擎还没报进度"，不编一个假百分比（这是 F 批的全部意义）
⑫ 同源部署（后端托管前端产物）：
      `webapp.mount_frontend(app)` 把 `frontend/dist` 挂到 `/`，于是 `http://<局域网IP>:8000/`
      同时给出页面与接口（前端 `apiUrl()` 退化成相对路径，CORS 用不上）；**挂载点必须写在 main.py 最后**；
      `/api`、`/files` 下的未知路径不参与 SPA 回退（保持 JSON 404），只有浏览器导航（`Accept: text/html`）才回退 `index.html`；
      `index.html` / `sw.js` / `manifest.webmanifest` 发 `Cache-Control: no-cache`；
      产物不存在就跳过挂载并打日志（开发态不构建也能起服务），启动日志顺便列出本机局域网地址
```

## 后续功能接入点（2026.9.8 定稿）

> **关键定稿决策**：①移动端**用 PWA**（不碰原生 Capacitor/APK——响应式网页可装主屏当 App 用，已核实同类非原生形态可获认可）；②**导读/问答用"全量入上下文"**（V4 约 1M 上下文，单篇 <10 万 token 整篇可行），**不建向量库/ChromaDB**；③**导读/术语惰性按需计算**（不走 worker、不建状态机）；④**加 `GET /understanding` 派生状态兜底**；⑤**加 `app/deps.py` 集中依赖注入**。

| 功能点 | 改哪个文件 | 加什么 |
|---|---|---|
| M0 LLM 质量预演（半天~1天） | 独立脚本 / `app/llm.py` | 首次真跑 DeepSeek，验"结构化 JSON 稳定性 + 导读/术语质量 + 单篇 token 成本"；**不验裁剪**（单篇不裁剪） |
| M1 导读/术语（理解层） | 新建 `app/llm.py` + `models.py` + `app/deps.py` | `extract_understanding(texts)->{guide(带 source_block_id), terms}`；**惰性按需**：用户打开"导读/术语"页签才调 LLM、缓存、失败显示"暂无导读"；**不走 worker、不建 enrich 状态机**；`GET /understanding` 派生状态兜底 |
| M2 阅读工作台（UI） | 先抽 composable（`useTaskRunner` 独 owner EventSource、`useTaskHistory`，**不装 Pinia**），再拆组件 | 结果区多 Tab（双语稿/导读/术语表/问答）；**精修 3 个 hero 屏**（双语对照/手机导读卡/档位权衡可视化）；手机加"点溯源"底部抽屉（原文+译文+高亮+出处） |
| M3 移动端（PWA）✅ | 前端 `manifest.webmanifest` + `sw.js` + `icons/` + `api.js`；后端 `config.py`/`main.py` CORS 白名单 | 已完成：可装主屏/全屏/离线壳（`/api`、`/files` 永不缓存）；**所有 `/api` 调用改 `apiUrl()` 绝对 base**（`VITE_API_BASE` 或 `?apiBase=` 或 localStorage）；后端 CORS 可用 `DOCWISE_CORS_ORIGINS`（逗号分隔）追加手机来源 |
| M4 论文问答 ✅ | 复用 `app/llm.py`（`answer_question`）+ 新 `app/routers/ask.py`；`db.py` 加 `ensure_fts`；前端问答 Tab | 已完成：`POST /api/tasks/{id}/ask`——译文总字符 ≤ `DOCWISE_ASK_FULL_CONTEXT_MAX_CHARS`（默认 30 万）**全量入上下文**；超阈值 **FTS5（trigram）召回 Top8 + 2 字词 LIKE 兜底**（**均限定本任务**）；回答按 block_id 引用、召不回诚实返回；**不建向量库**；FTS 表每次启动重建（内容源 task_blocks，触发器同步） |
| 补门：能力一致性 ✅ | 前端 `App.vue`（`openWorkbench`/`refreshWorkbench` 统一入口 + 工作台独立卡片）；`main.py` 任务详情；`routers/ask.py` 检索作用域 | 已完成：①**检索按任务过滤**（原先会拿别的论文的块当出处）+ 出处 id 存在性校验；②历史"继续读"与上传后**共用同一工作台**（双语稿/导读/术语表/问答 + 溯源），不再只给分块列表；③任务详情一次返回 `files_ready` 与 `understanding_status`（省掉 HEAD 探测与额外往返）；④术语表页签**自触发**生成、失败给"重试"按钮且不再静默重复调 LLM；⑤撤下未实现宣称（"质检一条龙"等）与陈旧页脚，统一 docwise 品牌口径 |
| 保命基建 ✅ | 新建 `logging_setup.py`；`worker.py`；`db.py`；`main.py` health；前端状态条 | 已完成：①**全仓首个日志系统**（`data/logs/docwise.log` 轮转，写不了文件自动降级控制台）；②**worker 异常不再杀死循环**（单任务异常落库为失败 + 记堆栈，循环继续）、空闲每 30s 扫表兜底、心跳暴露给健康检查；③`GET /api/health` **真检查**（DB 写探测 + 心跳新鲜度 + 队列长度 + 正在处理的任务），前端状态条据此显示"已连接/部分异常/排队几篇"；④SQLite **WAL + busy_timeout + synchronous=NORMAL**（文档早就写"WAL 常开"，代码里其实没有） |
| B 批：账号体系 ✅ | 新建 `app/auth.py` + `app/routers/auth.py` + `app/routers/admin.py` + `app/scripts/reset_password.py`；`models.py`（3 张表 + 任务归属列）；`deps.py`（守卫）；`main.py`（9 条路由加鉴权）；前端 `auth.js` + `LoginView.vue` + `AdminView.vue` | 已完成：①注册/登录/退出/改密 + 会话（库中只存 sha256、可吊销）+ **登录限流**（同用户名+IP 连错 5 次锁 5 分钟）；密码用标准库 scrypt（不引依赖）；②**每篇论文有主人**，所有任务接口按归属过滤、非本人 404；**删除**了 `POST /api/tasks`（传服务器路径的旧入口）；③**临时票据**解决"进度推送/PDF 预览/下载带不了请求头"；④**管理后台**（账号列表、任务总览、重置密码、停用启用、角色调整；不能停用/降级自己）；⑤演示一键登录（`DOCWISE_DEMO_AUTOLOGIN`）；⑥存量无主任务按 `DOCWISE_LEGACY_OWNER` 归属；⑦**测试改造**：conftest 加"默认已登录"覆盖 + 上传目录改到临时目录，新增 `test_auth/test_authz/test_admin`（未登录 401、跨用户 404、后台 403） |
| E 批：任务生命周期与运维 ✅ | 新建 `app/storage.py` + `app/routers/tasks.py` + `scripts/backup_db.py`；`engine/base.py`（CancelToken、output_dir、CANCELLED）；`engine/open_source.py`（Popen + 取消 + 日志）；`worker.py`（取消信号表）；`main.py` 瘦身；前端 `App.vue`（取消/重试/删除按钮 + 已取消状态） | 已完成并合并（**PR #50**）：①**取消正在跑的翻译**——`CancelToken` 从接口线程传到引擎线程，引擎轮询到就终止子进程（Windows 先 `taskkill /T` 再补 `terminate`，实测半秒级），worker 落库 `cancelled` 并推 SSE；排队中的任务走条件 UPDATE 直接置终态；②**失败重试**（failed/cancelled 可重试）：清旧块/旧导读/旧产物 → 回 pending → 重新入队，历史保留；③**删除任务**：先叫停 → 手工清 4 张子表（**SQLite 外键没开级联**）→ 删行 → 删上传原件与结果目录；④**磁盘治理**：结果统一落 `data/outputs/{task_id}`（原来散在系统临时目录），启动回收孤儿目录与陈旧临时目录；⑤**备份** `scripts/backup_db.py`（`VACUUM INTO` + 校验 + 只留最近 N 份；WAL 下直接拷 `.db` 会丢最近改动）；⑥**路由拆分** `main.py` → `routers/tasks.py`（main 只剩启动/健康检查/CORS/挂载）；⑦引擎日志落 `engine.log`，失败时把日志尾部写进任务卡；⑧顺手修了"上传过一次就不能再上传"（`n-upload` 的 `onFinish` 不清理内部列表，`max=1` 一直被判定已满） |
| F 批：诚实进度 ✅ | 新建 `app/progress.py`（解析引擎日志）+ `tests/test_progress.py`；`models.py`/`main.py` 加 `stage`/`eta_seconds`/`started_at`/`finished_at`；`worker.py`（进度搬运工）；`routers/tasks.py`（进度/耗时/队列位置字段）；前端 `App.vue`（阶段文字 + 已耗时 + 引擎进度/预计剩余，拿不到就显示"引擎还没报进度"） | 已完成：①**真进度来自引擎自己**——引擎的 tqdm 进度条（`2/10 [00:01<00:05, 1.44it/s]`，单位是页）本来就打在日志里，现在被解析出来；②worker 每 1.5s 把进度搬进库并推 SSE（**只增不减**，避免引擎分阶段重跑时进度条跳回 0）；③任务详情暴露 `engine_progress`（当场解析，最新）、`elapsed_seconds`、`queue_position`（排队时"前面还有几篇"）；④前端：队列中显示"排队中 · 前面还有 N 篇"，跑起来显示"正在翻译 · 第 3/10 页 · 1.24 页/秒 · 已用 12 秒 · 引擎预计还需 25 秒"，**解析不出就显示"引擎还没报进度"并画转圈**（不编百分比）；⑤顺手把 `**/data/` 整个加进 `.gitignore`（E 批新增的 `data/outputs/`、`data/backups/` 原先没被忽略，`git add .` 会把用户的论文、引擎日志和数据库备份提交进公开仓库） |

## 关键观察（改动时注意）

1. **LLM 客户端在 `app/llm.py`（M0 起已接）**：`extract_understanding`（导读/术语）+ `answer_question`（问答）；两者都是**同步直调、不进 worker、不缓存**，配置走 `DEEPSEEK_*`（`config.py` 只放配置，调用都在 `llm.py`）。
2. **路由组织**：`app/deps.py` 集中依赖注入（get_db 再导出 + get_settings）；路由全部在 `app/routers/`（`tasks.py` 任务与进度 / `ask.py` 问答 / `auth.py` 账号 / `admin.py` 后台），`main.py` 只留启动初始化、健康检查、CORS 与路由挂载——**新增任务相关接口请写进 `routers/tasks.py`，别再往 main.py 里加**。
3. **App.vue 是单文件巨组件**：先抽 composable（状态边界）再拆组件；用 `useTaskRunner`/`useTaskHistory`，**不装 Pinia**；再拆 `ReadingWorkbench`/`GuideTab`/`TermsTab`/`AskTab`/`HistoryList`。
4. **worker 单进程串行 + SQLite 单写者**：所有写库**收敛到一条写路径**；富化**不进 worker 任务状态机**（改惰性按需）；WAL 常开但**不背书并发写**。
5. **SSE 靠进程内事件总线（单进程）**：`worker` 与 SSE 端点共享 `app.state.event_bus`，**勿用 `--workers N` 多 worker**、**勿引入跨进程 pub/sub**（过度设计）；`understanding` 状态一律走 `GET /understanding` 派生，**不新增专用 SSE 事件**。
6. **precise 档当前映射 medium**（`registry.py` 里 `Tier.PRECISE: MediumEngine`），精细引擎未真正接入。
7. **无质量/回退/打回逻辑**：属后续阶段（2027），当前不加，答辩别吹。
8. **迁移**：保留 `create_all` + `_ensure_schema`（`PRAGMA table_info` + ALTER ADD 缺列），**不用 Alembic**；对真实存量库手测一次。
9. **不引入重基建**：**不建向量库/ChromaDB、不装 Pinia、不引入 Alembic、不引入 Capacitor**（用 PWA）——都是为"1 人现场答辩 + 9 月交稿"控成本。
10. **前端 API 调用一律走 `apiUrl()`**（`src/api.js`）：base 优先级 `?apiBase=` > localStorage（`docwise_api_base`）> `VITE_API_BASE` > 同源相对路径；手机连电脑后端用绝对 base，并在后端 `.env` 加 `DOCWISE_CORS_ORIGINS`（逗号分隔）放行手机页面来源。**SW 只缓存应用壳，`/api`、`/files` 直连不缓存**；新增后端路由不会被 SW 拦截。
11. **问答检索（M4）**：FTS5 用 **trigram** 分词器（unicode61 不拆 CJK，`实*` 零命中；trigram 最少 3 字符）——查询词 = ASCII≥3 字 + CJK 三字滑窗（去虚词）；2 字词（如"方法"）trigram 查不到，走 `translated/text LIKE %词%` 兜底；FTS 表每次启动 DROP+重建（rebuild 回填，容忍分词器/结构变更，开发库代价可忽略）。**问答不进 worker、不缓存、同步直调 LLM**。
12. **检索必须限定任务**（已修的坑）：`task_blocks_fts` 是外部内容表（rowid = `task_blocks.id`），查询要 `JOIN task_blocks tb ON tb.id = f.rowid AND tb.task_id = :task_id`；**block_id 跨任务会重名**（每个任务都有 b1/p0_b0），不过滤会把别的论文的块当本论文出处、而前端按当前任务解析后**看不出引错**。另外 SQLite 下 `bm25()` 必须写表真名（`bm25(task_blocks_fts)`），写别名会报错并被降级分支吞掉。
13. **前端只有一个工作台入口**：`openWorkbench(task)`（历史"继续读"与上传后共用）→ `refreshWorkbench(taskId)` 一次取全（blocks + files_ready + understanding_status）；别处不要再各写一套加载逻辑。理解层保持惰性：打开任务只 GET，用户点"导读/术语表"页签才 POST 生成，且每个任务只自动试一次（失败由"重试"按钮显式触发）。
14. **韧性约定（保命基建）**：① **单个任务的任何异常都不得杀死 worker 循环**（`_loop` 里 try/except + `_fail_safely`）；② 新增后台逻辑时要 `logger` 打点（入队/开始/结束用时/失败堆栈），**别再出现"出事没有记录"**；③ 健康检查必须**真验证**（现在会真写一次库并读 worker 心跳），不要写死返回 ok；④ `setup_logging()` 在 `main.py` 导入时调用一次（幂等），测试里也有覆盖。
15. **鉴权与归属约定（B 批）**：① 新增任何"按任务"的接口，必须用 `load_owned_task(db, user, task_id)` 校验归属（**返回 404 而不是 403**，不暴露"存在但无权限"）；② 新增"浏览器自己发起"的接口（iframe / `<a href>` / EventSource 之类）时，鉴权必须走 `get_user_for_files` / `get_user_for_events`（它们接受 `?ticket=`），**不能只认请求头**；③ 前端新增请求一律用 `authFetch`（自动带令牌 + 401 回登录页）；新增上述三种 URL 一律用 `urlWithTicket()`，不要手拼；④ **票据不一次性消费**（60 秒内可重复使用），否则同一页面多次取 URL 会互相把票用掉、表现为"有时打不开"。
16. **测试约定（B 批）**：功能测试默认"已登录"（conftest 覆盖 `get_current_user`/`get_user_for_files`/`get_user_for_events` 返回固定用户，账号 id 见 `tests/helpers.py` 的 `TEST_USER_ID`）；**直接建任务时必须带 `user_id=TEST_USER_ID`**；鉴权类测试（`test_auth`/`test_authz`/`test_admin`）不走覆盖、用真实令牌；上传目录在测试里被指向临时目录，避免写仓库 `data/`。
17. **任务生命周期约定（E 批）**：① **取消不算失败**：状态是第四个终态 `cancelled`，SSE、前端 `TERMINAL_STATES`、健康检查都要认它（漏一处的表现是"进度条卡住不再更新"）；② 取消信号只由 `worker.request_cancel(task_id)` 发，**滚动库里落库由 worker 做**——接口层别抢着写终态，否则会和 worker 的 `completed/failed` 落库打架；③ **数据库外键没开级联**（SQLite `PRAGMA foreign_keys` 默认关，模型里也没写 `ondelete`），所以删任务必须在代码里手工删 `task_blocks`/`task_history`/`task_understanding`/`auth_tickets`；④ 结果文件一律落在 `storage.outputs_dir(task_id)`（**不要用 `tempfile.mkdtemp`**，散落的临时目录没法按任务清理；受限环境里 mkdtemp 建出来的目录还写不进去）；⑤ 引擎子进程用 `Popen` 而不是 `subprocess.run`——只有 Popen 才能中途终止；输出重定向到 `engine.log`（**不要用管道**：管道写满会互相卡死，日志也留不下来）；⑥ 删除/清理文件必须过 `storage.remove_file/remove_dir`，它们只删 `data/` 以内（防记录里的路径被人改过、删到系统文件）。
18. **诚实进度约定（F 批，含两次踩坑后的定稿）**：① **不许编进度**——解析不出引擎进度就说"引擎还没报进度"，宁可显示不确定，也不要一个假装在走的百分比；② 进度数据源只有一个：引擎自己打到 `engine.log` 的 tqdm 进度条，`progress.py` 负责解析；**tqdm 用 `\r` 原地重绘**，所以必须按 `[\r\n]+` 切开取最后一条（按行读只会读到第一条）；进度条单位是**页**（引擎侧 `tqdm(total=total_pages)`）；③ **子进程输出必须不缓冲**：`open_source.py` 给引擎子进程设 `PYTHONUNBUFFERED=1`——stderr 重定向到文件时是块缓冲的，而进度条本来就"停在原地"（`\r` 不换行），不设的话进度会在缓冲里攒着、快结束才一次性落盘；④ **但别指望引擎的进度条会平滑**：实测同一台引擎、10 页论文，中途可能十几秒不更新一条（只有开头和结尾各刷一次）。**所以"已用时间"才是主指标**（`started_at`/`finished_at` 由 worker 写、前端本地走秒），引擎报的页数一律标成"**引擎自报**"（是引擎说的，不是我们的承诺）；⑤ **进度条永远要在**：不知道百分比时画一根流动的条纹条（`show-indicator` 只在 `indicator-placement="outside"` 下生效，所以"不写百分比"这一支别用 inside 指示器）——曾经"没数字就不画"，用户看到的就是"进度条没了"；⑥ 落库时**进度只增不减**（引擎分阶段重跑时进度条会回到 0，跳回去像坏了）；⑦ 读日志只读尾部（`read_log_tail`，默认 8KB）；⑧ 新增"进度类"字段时，前端三处都要认：SSE 事件、轮询兜底、详情接口。
19. **前端渲染约定（前端测试的由来）**：① **模板里要渲染的值一律用 `computed` / `ref`，不要用普通函数**——模板插值一个函数名（少写一对括号）会被 Vue `String(fn)` 把整个函数源码印在页面上，而 `vite build` 完全不报错（F 批真出过这个事故，是用户截图发现的）；② 所以前端加了**渲染冒烟测试**（`frontend/tests/app.smoke.test.js`，vitest + happy-dom）：真挂载 `App.vue`、打开一篇任务，断言页面文本不含 `function `、`=>`、`{{` 这类源码痕迹；③ 文案逻辑抽到 `src/progressText.js` 做纯函数单测，App.vue 只包一层 computed；④ 改前端后请跑 `cd frontend && bun run test`（首次需要 `bun install`，依赖已在 package.json 里）；⑤ **改了 `sw.js` 或应用壳记得把 `CACHE_NAME` 升一位**，否则用户会一直吃旧壳、刷新也看不到新版本。
20. **同源部署约定（后端托管前端产物）**：① `webapp.mount_frontend(app)` **必须写在 `main.py` 的最后**——Starlette 按注册顺序匹配，挂在 `/` 的静态托管若写在前面，会把后面定义的路由（含 `/api/health`）一起当成静态文件；② 回退时捕获的必须是**Starlette 的 `HTTPException`**：`StaticFiles` 抛的是它，而 `fastapi.HTTPException` 是它的**子类**，写成 FastAPI 那个就永远接不住 404、SPA 回退静默失效（本地实测踩过一次，是测试抓出来的）；③ `/api`、`/files` 下的未知路径**不参与**回退（保持 JSON 404，回退成 HTML 会让前端错误处理拿到页面源码），且只有 `Accept: text/html` 的浏览器导航才回退，拼错的 `/assets/*.js` 老老实实 404；④ `index.html`/`sw.js`/`manifest.webmanifest` 发 `Cache-Control: no-cache`（它们文件名不变，启发式缓存会让装了 PWA 的手机吃旧壳）；⑤ 产物不存在只跳过挂载 + 打日志（开发态不构建也能起服务），`DOCWISE_SERVE_FRONTEND=0` 关闭、`DOCWISE_FRONTEND_DIST` 改路径（相对路径按**仓库根**解析）；⑥ 前端改完要**重新构建** `frontend/dist`，后端只负责端出去、不会替你编译；⑦ 回归测试在 `tests/test_same_origin.py`（含"挂载点之后不许再有 `/api`、`/files` 路由"的结构性断言）。
